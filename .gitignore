# build output
android/build/
android/app/build/
android/.gradle/
android/local.properties
*.apk
*.aab

# signing keys never belong in the repository
*.keystore
*.jks
android/keystore.properties

# tooling
node_modules/
.DS_Store

# the test harness symlinks the fixtures into www/ so the browser can fetch
# them, and removes it on exit -- a cancelled run leaves it behind
www/fixtures
